```python 

textent_metadata_df.document_length.describe()

textent_metadata_df.document_length.plot(kind='hist', bins=50, figsize=(6, 4), title='Document length distribution')

# to see the outliers
textent_metadata_df[['document_length']].boxplot()

mean = textent_metadata_df.document_length.describe().loc['mean']
std = textent_metadata_df.document_length.describe().loc['std']
iqr = (mean - 1.5 * std, mean + 1.5 * std)
textent_metadata_df[textent_metadata_df['document_length'] < iqr[0]]
textent_metadata_df[textent_metadata_df['document_length'] > iqr[1]][['title']].to_dict()
```